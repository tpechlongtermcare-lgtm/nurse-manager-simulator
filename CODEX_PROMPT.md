# 給 Codex 的接手提示詞

你正在維護一款純前端、資料驅動的繁體中文回合制遊戲《護理長模擬器：今天也沒有標準答案》。

## 不可破壞的原則

1. 事件、NPC、行動、運勢、成就、結局內容都從 `data/*.json` 讀取。
2. 新增一般事件卡不得要求修改 JS。
3. 不加入 npm、bundler、框架或後端。
4. 保持 GitHub Pages 可部署。
5. 手機 375px 優先。
6. 視覺是「護理站交班本＋生活模擬遊戲感」，不要改成 SaaS dashboard。
7. 不使用《模擬市民》的商標、角色、圖像、UI 素材或直接複製其 trade dress；只保留生活模擬／角色狀態管理的遊戲感。
8. 所有人物、事件與機構必須維持虛構，不加入可辨識真人或真實機構資料。
9. `prefers-reduced-motion` 必須繼續支援。
10. 修改後要至少測試：30 天流程、AP、follow-up、NPC 離職、即死結局、第 30 天評鑑。

## 現有架構

- `src/main.js`：載入資料、畫面流程
- `src/engine.js`：回合、AP、事件、結算、結局、成就
- `src/state.js`：狀態與 localStorage
- `src/deck.js`：條件、加權抽卡、NPC 文字代入
- `src/staff.js`：NPC 效果、恢復、離職
- `src/effects.js`：數值效果、條件比較
- `src/ui.js`：渲染與互動
- `data/events.json`：22 張種子事件

## 下一步建議

優先順序：

1. 把事件擴充到 60+ 張，維持日常／危機／follow-up 比例。
2. 增加「人物關係感」但不要變成複雜 RPG：事件結果可讓特定 NPC 留下一句反應。
3. 加入很輕的環境音效開關：翻紙、印章、電話震動；預設關閉或尊重使用者設定。
4. 做事件內容驗證器，在開發模式檢查 JSON 缺欄位、重複 id、followUp 指向不存在事件。
5. 加入 deterministic seed，方便企劃重現某一局除錯。
6. 不要增加教條式提示，不要告訴玩家哪個選項「正確」。

若我提出新功能，先判斷能否做成 JSON 欄位或通用規則；可以資料驅動就不要寫死 id。
